specVersion: "v1beta"
serviceId: "to-do_list"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "to-do_list-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
